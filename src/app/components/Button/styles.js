
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderWidth: 1,
    borderColor: 'white',
    borderRadius: 4,
    padding: 18,
    marginTop: 30
  },
  buttonText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white'
  }
});

export default styles;
